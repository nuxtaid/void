# Scripts

This folder contains local helper scripts only.

## Notes

- These files are not required for the Nuxt layer to run.
- They are not imported by the application code in this repository.
- They are not part of the published `@nuxtaid/void` package API.

At the moment, the Python files here appear to be unrelated utility or experiment scripts rather than runtime dependencies for the layer or the `.playground` app.

If a script becomes part of the actual development workflow, document its command and purpose here.
